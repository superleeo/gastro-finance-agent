# 🍽️ Gastro Finance Agent — 中文使用指南

> 德国餐饮业自主财务自动化与 AI 可视化系统 · v1.0.0

---

## 一、这个系统是干什么的？

如果你在德国开餐厅、咖啡馆、外卖店、小吃店（Imbiss），**Gastro Finance Agent** 能帮你自动完成以下事情，省掉大量的手工记账和 Excel 操作：

| 痛点 | 系统怎么解决 |
|------|-------------|
| 每天收银小票一大堆，手动录入 Excel 累死人 | 拍张照 → 本地 AI 自动识别 → 财务数据自动入库 |
| 银行流水和供应商发票对不上，不知道哪笔没发票 | 上传银行 CSV → 自动匹配 → 缺发票的自动列出 |
| 缺发票要找供应商要，写邮件麻烦 | 自动生成标准德语催收邮件，一键发送 |
| 不知道这个月赚了多少、要交多少税 | 日/周/月/年可视化图表，实时看利润和税务 |
| 搞不清注册 GmbH 还是个体经营划算 | 内置德国税法计算器，对比两种模式的税后收入 |

---

## 二、系统架构

```
你的餐厅数据                         系统处理                           你看到的
──────────────────────────────────────────────────────────────────────────
📸 收银小票照片          →   zbon_parser.py (AI识别+财务校验)    →
🏦 银行流水 CSV          →   reconciliation.py (自动对账)        →   Streamlit 网页看板
📋 供应商发票 CSV        →   services.py (模糊匹配)             →   手机/电脑都能看
⚙️ .env 配置文件         →   config.py (餐厅品牌+税务设置)      →
```

**技术路线：** 所有数据处理都在你本地电脑上完成，不联网、不上传云端，数据安全可控。

---

## 三、能做什么？逐个功能详解

### 3.1 📸 收银小票自动解析（zbon_parser.py）

**解决的问题：** 每天打烊后，收银机出一张汇总小票（Z-Bon），上面有几十条菜品、饮品，税率还不一样（食品 7%、饮品 19%）。手工录入 Excel 不仅慢，还容易算错。

**系统做法：**
1. 用手机拍下小票照片，放到 `input_raw/` 文件夹
2. 本地 VLM（视觉大模型，比如 Ollama + LLaVA）读取照片，提取出结构化的 JSON 数据
3. `zbon_parser.py` 对 JSON 做严格财务校验：
   - 每一条：**净值 + 税额 = 总额**（误差不超过 €0.02）
   - 总账：**现金 + 刷卡 = 总营业额**
   - 税率分离：自动区分 7% 食品和 19% 饮品
4. 校验通过的数据存入系统，供日报表/月报表使用

```python
# 示例：VLM 从小票照片中提取的 JSON
{
  "receipt_date": "2026-06-05",
  "items": [
    {"name": "味噌拉面", "category": "speisen", "netto": 10.00, "brutto": 10.70},
    {"name": "朝日啤酒", "category": "getraenke", "netto": 4.00, "brutto": 4.76}
  ],
  "payments": [
    {"method": "cash", "amount": 10.00},
    {"method": "card", "amount": 5.46}
  ]
}
```

### 3.2 🏦 银行流水自动对账（reconciliation.py）

**解决的问题：** 每个月收到银行账单，几十笔扣款，哪些有发票、哪些没有，人工核对极其耗时。

**系统做法：**
1. 从网银下载 CSV 文件（支持德意志银行、Sparkasse、Commerzbank 等主流银行格式）
2. 放到 `input_bank/` 文件夹
3. 系统自动解析（兼容德语分号分隔、逗号小数点、尾置负号等德国特有格式）
4. 与系统已有发票记录做**模糊匹配**（即使供应商名字不完全一样也能匹配上）
5. 匹配不上的自动标记为**缺失发票**

```bash
# 支持的数字格式示例
"1.234,56"   → 1234.56 欧元  （德国标准）
"-1.234,56"  → -1234.56 欧元 （前置负号）
"1.234,56-"  → -1234.56 欧元 （DTAUS 尾置负号，德意志银行常用）
```

### 3.3 📧 自动催收邮件（reconciliation.py）

**解决的问题：** 发现缺少发票后，要逐一手动写邮件给供应商，费时费力。

**系统做法：**
1. 对账完成后，自动识别缺失发票的供应商
2. 一键生成标准德语商务催收邮件
3. 邮件包含：交易日期、收款方、金额、用途、你的餐厅联系方式
4. 邮件保存为 `.txt` 文件在 `email_drafts/` 目录下，可以直接复制发送

```
生成的邮件示例：
────────────────────────────────────
Betreff: Bitte um Rechnungszusendung — Metro C&C / Buchung vom 2026-06-02

Sehr geehrte Damen und Herren,

bei der Durchsicht unserer Bankkontobewegungen ist uns aufgefallen,
dass wir für die folgende Abbuchung bislang noch keine Rechnung erhalten haben:

  • Buchungstag:   2026-06-02
  • Empfänger:     Metro C&C Düsseldorf
  • Betrag:        1,240.00 EUR
  • Verwendungszweck: Einkauf Lebensmittel

Wir bitten Sie, uns die zugehörige Rechnung schnellstmöglich zukommen
zu lassen.

Mit freundlichen Grüßen,
[你的餐厅名称] — Finanzabteilung
Tel: [你的电话]
────────────────────────────────────
```

### 3.4 📊 网页看板（dashboard.py + finanz_dashboard.py）

**解决的问题：** 老板/经理需要随时了解餐厅经营情况，但没有一个直观的界面。

**系统提供 6 个页面：**

| 页面 | 内容 |
|------|------|
| **概览** | 核心指标卡片（累计对账数、AI 准确率、异常项数、现金流预估）+ 财务健康图 + 待处理清单 |
| **Finanzanalyse** | 营业额构成饼图（食品 vs 饮品）、成本结构柱状图、EBITDA 趋势折线图、GmbH vs 个体经营税务对比、AI 智能建议 |
| **智能对账** | 交易比对表（POS 金额 vs 银行金额）+ AI 分析 + 一键修复 |
| **供应商** | 已登记发票列表 + 上传银行 CSV + 自动对账 + 一键生成催收邮件 |
| **任务日志** | AI 任务的执行记录（时间/任务/状态/详情） |
| **报表** | 德国增值税预申报（UStVA）+ 邮件草稿管理 |

**Finanzanalyse 页面功能详解：**

- **时间维度切换：** 日 / 周 / 月 / 年 — 点一下按钮就能切换
- **企业形式切换：** GmbH（有限责任公司）vs Einzelunternehmen（个体经营）
- **5 个 KPI 指标卡：** 营业额、总成本、EBITDA、利润率%、预估税款
- **营业额饼图：** 食品 7% 税率和饮品 19% 税率各自占比
- **成本横向柱状图：** 食材、人工、租金、能源、保险、营销、其他 — 各自占营业额的百分比
- **趋势折线图：** 营业额 + 成本 + EBITDA 三条线，一目了然
- **税务对比图：** 同样利润下，GmbH 交多少税 vs 个体经营交多少税
- **AI 建议面板：** 系统自动分析数据并给出建议，比如：
  - "利润率仅 5%，行业平均 10-18%，建议优化菜单和供应商价格"
  - "人工成本占比 35%，超过行业目标 28-32%，建议优化排班"
  - "当前利润水平适合考虑注册 GmbH 以获得税务优势"

### 3.5 🧾 税务计算引擎（financial_analytics.py）

**解决的问题：** 德国税法复杂，搞不清到底该交多少种税、每种多少。

**支持的税种计算：**

| 税种 | GmbH | 个体经营 | 说明 |
|------|------|---------|------|
| 增值税（USt） | 食品 7% / 饮品 19% | 同左 | 在收银阶段已分离 |
| 营业税（GewSt） | 3.5% × 地方稽征率 | 同左 | 默认按 400% Hebesatz |
| 公司税（KSt） | 15% | — | 仅 GmbH |
| 团结税（Soli） | 5.5% × KSt | — | 仅 GmbH |
| 个人所得税（ESt） | — | 14%-45% 累进 | 仅个体经营 |

**示例：月 EBITDA €46,000 时**

```
GmbH 模式：                      个体经营模式：
├ 营业税     €6,440               ├ 营业税     €6,440
├ 公司税     €5,934               └ 个税       €8,113
├ 团结税     €326                 合计:        €14,553
├ 合计:      €12,700              ─────────────────
└ 净利润:    €33,300              净利润:      €31,447
  税率:      27.6%                 税率:        31.6%
```

### 3.6 🔒 德国合规（GoBD）

- 日志自动轮转（10MB × 30 份），保留完整审计记录
- 财务计算使用 `Decimal`（精确十进制），不是 `float`（浮点近似），确保不会出现 1 分钱的误差
- 数据本地存储，不上传第三方服务器

---

## 四、怎么安装？

### 前提条件
- 电脑：Mac / Windows / Linux 都可以
- Python：3.11 或更高版本
- 终端：会用命令行（不会的话找年轻人帮忙，5 分钟的事）

### 安装步骤

```bash
# 第 1 步：下载代码
git clone https://github.com/superleeo/gastro-finance-agent.git
cd gastro-finance-agent

# 第 2 步：安装依赖（第一次需要 1-2 分钟）
pip install -r requirements.txt

# 第 3 步：配置你的餐厅信息
cp .env.example .env
# 用文本编辑器打开 .env 文件，改成你自己的：
#   GASTRO_RESTAURANT_NAME="你的餐厅名字"
#   GASTRO_RESTAURANT_EMAIL="你的邮箱"
#   GASTRO_RESTAURANT_PHONE="你的电话"

# 第 4 步：启动系统
GASTRO_DEMO_MODE=1 streamlit run dashboard.py
# 浏览器会自动打开 http://localhost:8502
```

### 如果不想用演示数据

把 `.env` 里的 `GASTRO_DEMO_MODE=1` 改成 `GASTRO_DEMO_MODE=0`，然后在看板里上传你自己的银行 CSV 和发票 CSV。

### 验证安装是否成功

```bash
# 运行 39 个自动化测试
python3 -m pytest tests/ -v

# 应该看到 39 passed，像这样：
# tests/test_zbon_parser.py ................             [ 41%]
# tests/test_reconciliation.py .......................    [100%]
# ======================= 39 passed in 0.10s =======================
```

---

## 五、日常使用流程

### 每天
1. 打烊后，手机拍小票 → 放到 `input_raw/`
2. 打开看板 → 概览页面看昨天的营业数据

### 每周
1. 从网银下载本周 CSV → 放到 `input_bank/`
2. 打开看板 → 供应商页面 → 上传 CSV → 自动对账
3. 如果有缺失发票 → 一键生成催收邮件 → 复制发送

### 每月
1. 打开看板 → Finanzanalyse → 切换到"月"视图
2. 查看营业额趋势、成本比例、利润率
3. 对比 GmbH 和个体经营的税务差异
4. 查看 AI 建议：有没有可以优化的地方
5. 报表页面 → 导出 UStVA 报告给税务顾问

### 每年
1. 切换到"年"视图，查看全年数据
2. 导出 Excel 报表给税务顾问做年报
3. 对比往年数据判断业务增长趋势

---

## 六、常见问题

**Q: 这个系统需要联网吗？**
A: 日常使用不需要。银行对账、财务报表、看板都在本地运行。只有当你使用本地 VLM 识别小票时需要本地 AI 模型（也是本地运行）。

**Q: 支持哪些德国银行？**
A: 目前支持德意志银行、Sparkasse、Commerzbank 等主流银行的 CSV 导出格式。如果你的银行格式不支持，可以提交 issue。

**Q: 数据安全吗？**
A: 所有数据都在你的电脑上，不经过任何第三方服务器。日志文件有轮转机制，敏感目录（银行数据、邮件草稿）已被 `.gitignore` 排除，不会被意外提交到网上。

**Q: 如果我有多个门店？**
A: 目前版本是单店模式。多门店支持在路线图中。

**Q: 税务计算准确吗？**
A: 税率按照 2026 年德国现行税法。但仅供参考——正式报税请务必咨询持牌税务顾问（Steuerberater）。

---

## 七、文件结构速查

```
gastro-finance-agent/
│
├── 📄 配置层
│   ├── .env.example        ← 餐厅信息模板（复制为 .env）
│   ├── config.py           ← 全局配置 + 税务常量
│   └── pyproject.toml      ← Python 项目配置
│
├── 🧠 业务逻辑层
│   ├── zbon_parser.py      ← 小票 AI 识别 + 财务校验
│   ├── vlm_schema.py       ← VLM 输入数据格式定义
│   ├── reconciliation.py   ← 银行对账 + 模糊匹配 + 催收邮件
│   ├── services.py         ← 业务服务层
│   ├── financial_analytics.py ← 税务计算引擎
│   └── tax_report.py       ← 德国增值税预申报导出
│
├── 🖥 展示层
│   ├── dashboard.py        ← 6 页网页看板（主程序）
│   └── finanz_dashboard.py ← 可视化分析页面
│
├── 🧪 测试层
│   └── tests/
│       ├── conftest.py
│       ├── test_zbon_parser.py      (16 个测试)
│       └── test_reconciliation.py   (23 个测试)
│
└── 📂 数据目录
    ├── input_raw/          ← 放收银小票照片
    ├── input_bank/         ← 放银行 CSV
    ├── output/             ← 生成的 Excel/PDF 报表
    ├── email_drafts/       ← 催收邮件草稿
    ├── organized_data/     ← 归档数据
    └── logs/               ← 系统日志
```

---

**启动命令速记：**

```bash
cd gastro-finance-agent
GASTRO_DEMO_MODE=1 streamlit run dashboard.py   # 演示模式，含模拟数据
python3 -m pytest tests/ -v                      # 运行测试
python3 -m zbon_parser                            # 小票解析 Demo
python3 -m reconciliation                         # 银行对账 Demo
python3 -m financial_analytics                    # 税务计算 Demo
```
